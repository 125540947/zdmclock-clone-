node_modules
dist-ssr
.npmcache
.npmcache2
*.local
# 第三方/代理运行时目录（如 WorkBuddy 的 .hermes）：体积巨大，切勿提交
.hermes
.env
.env.generated
server/data/*.json
server/data_demo/*.json
server/data_test/*.json
# web/dist 预构建产物保留入库，使克隆后 `npm start` 无需先 build 即可运行
!web/dist
.DS_Store
npm-debug.log*
