# 值得吗签到助手全面审计报告（2026-09-04）

## 一、结论

项目当前“可运行、可回归”，但不建议在现有配置下直接作为公网开放服务长期运行。代码测试与构建质量已经达到可交付水平；生产环境仍有四项高优先级风险：调试日志泄露第三方响应隐私、数据库文件权限过宽、开放模式与公网直连组合风险、历史自动评论已经留下不友好的公开内容。

## 二、审计范围与证据

- 代码范围：`server/`、`web/`、部署配置、任务执行/鉴权/AI/通知/抓取链路。
- 本地验证：服务端 549 项通过、前端 35 项通过；生产构建通过；全量 ESLint 已通过。
- 线上验证：`zdmclock` systemd 服务 active，`/api/health` 返回 `ok=true`、`degraded=false`；线上提交为 `b7038a8`，本地仅多一个零副作用验证脚本提交。
- 线上当日明细：成功 12、失败 0、跳过 1；每日任务已显示完成/领取/跳过明细，众测来源限制被标为“跳过”而不是 Cookie 失效。
- 依赖审计：默认 npm 镜像返回 `NOT_IMPLEMENTED`，切换官方 registry 后请求超时，依赖漏洞状态本轮未能取得权威结果。
- UI 证据：通过本地 SSH 隧道查看线上页面，覆盖签到空状态、任务页、管理员登录弹窗、执行明细页。未输入管理员 Token，因此管理员后台、账号录入后的页面和 AI 配置页未做真实操作审计。

## 三、流程审计

1. **签到首页：基本健康**
   - 空账号状态有明确下一步“去录入账号”。
   - 风险：审计访问来自隧道回环地址，/24 网段隔离会显示“还没有账号”，真实用户若更换网络也可能产生同样困惑；建议显示“当前网络不可见”而不是“没有账号”。

2. **自动任务页：部分健康**
   - 自动任务、抓包导入入口可见。
   - 风险：页面仍展示 `server/captures/`、`node tools/importCapture.mjs` 等命令行步骤，和“你不用懂抓包”的产品承诺不一致；应提供上传/扫描状态和失败原因，不把终端命令作为主路径。

3. **管理员登录：需改进**
   - Token 未被回显，错误提示可见。
   - 风险：弹窗没有明显关闭按钮，Escape 未关闭；输入框在可访问性树中没有可读名称（可见 label 未通过 `for/id` 关联），键盘/读屏用户难以完成登录。应补 `role=dialog`、`aria-modal`、关联 label、关闭按钮和 Escape 关闭。

4. **执行明细：基本健康但信息密度偏高**
   - 今日汇总、任务状态、失败过滤、CSV/JSON 导出和逐篇评论详情均可见；每日任务失败/跳过明细已在新代码中接线。
   - 风险：默认只看“今天”，历史失败需要手动切换“全部”；移动端长句卡片可读性较差。建议默认保留最近 7 天入口，并将失败项名称单独分行。

## 四、高优先级发现

### P1-安全：生产调试日志已开启

线上 `SMZDM_DEBUG=1`。日志中出现完整第三方接口响应片段，包括 smzdm_id、昵称、地区、勋章及评论正文。虽然未发现 Cookie 明文，但这些内容仍属于不必要的个人/运营数据暴露，并会长期留在 journald/备份中。

**建议：**立即关闭 `SMZDM_DEBUG`，清理或轮转既有日志；将调试日志改为字段白名单和脱敏后的结构化日志。

### P1-安全：数据库权限过宽

线上 `data/db.json` 当前为 `zdm:zdm 644`，文件包含 Cookie、推送凭据和 AI 配置。服务账号只需读写自身文件，其他本机用户不应可读。

**建议：**运行时强制 `chmod 640`（目录 750），设置合适 umask，并在原子替换后再次保持权限；部署检查应把权限作为失败条件。

### P1-安全：开放模式 + 公网直连

线上同时为 `OPEN_MODE=true`、`BIND_ADDRESS` 未配置（默认监听 `0.0.0.0`）。这会让匿名访客可访问开放模式允许的读写入口；即使有 IP 限流，也不适合作为公网长期暴露的管理面板。

**建议：**公网部署改为 `OPEN_MODE=false`，设置强管理员凭据/API Token，后端绑定 `127.0.0.1` 并置于 HTTPS 反向代理后；同时显式配置 `PUBLIC_BASE_URL` 与 `HOST_ALLOWLIST`。

### P1-运营：历史自动评论仍有攻击性措辞

线上历史评论包含“糊弄谁呢”“累不累啊”“这是卖啥啊”等质问/嘲讽。最新代码已加入占位标题跳过、语气拦截和重试，但这些历史内容不会自动撤回；审计不能把历史公开结果视为已修复。

**建议：**暂停自动发布，先用零副作用脚本抽样生成并人工审核；增加“只生成草稿、人工确认后发布”默认值；对已发布历史内容建立人工复核/撤回清单。正则拦截应升级为“必须引用标题/价格中的具体事实 + 中性语气”的双重门槛。

### P1-数据正确性：频道回退可能造成假成功

日志多次显示文章详情接口返回 104 后退回 `channel_id=1`，随后收藏/点赞接口仍返回成功。该回退可能对错误频道发起动作，导致任务被记为成功但实际未作用于目标文章。

**建议：**对详情 104 默认标为“无法确认目标”，不要静默回退；只有接口返回明确目标文章且动作响应可验证时才记成功，并在明细中显示“频道未确认”。

## 五、中优先级发现

- `npm audit` 受镜像/网络阻断，当前不能证明依赖无漏洞；CI 应固定官方 registry 或内部可用镜像并保存审计结果。
- 生产服务和本地代码应在部署后自动记录 commit、构建时间和配置摘要（不含密钥），减少“代码已改但线上仍旧版”的排查成本。
- `server/tools/verifyToneZeroSideEffect.mjs` 目前是未跟踪文件；应决定纳入仓库并接入 CI，或明确放入忽略目录，避免验证能力只存在于单机。
- 页面已能展示每日任务的失败/跳过明细，但历史 taskRuns 仍保留旧的笼统文案；建议提供“历史记录不可补全”的标识，避免用户误以为旧数据也具备逐项原因。

## 六、已完成的本轮质量修复

- 修复 ESLint 的 3 个空 catch 和 3 个未使用参数告警；现在 `npm run lint` 全量通过。
- 未改动用户数据、Cookie、线上任务配置或自动发布状态。

## 七、建议实施顺序

1. 立即关闭调试日志、收紧 db.json 权限、停止公网开放模式。
2. 暂停自动评论发布，完成真实好价样本的零副作用生成验收。
3. 修复频道确认与动作成功判定，补充对应回归测试。
4. 改进管理员弹窗可访问性和空账号/网段隔离提示。
5. 恢复可用的依赖漏洞扫描，并把测试、lint、build、扫描接入部署门禁。

## 八、审计限制

本轮没有使用管理员 Token，也没有执行真实签到、评论、收藏、点赞、账号录入或第三方推送操作；因此这些写操作的实时 UI 反馈、权限边界和外部平台最终结果仍需在隔离测试账号上单独验收。
