# zdmclock-clone 傻瓜式部署指南（零基础版）

> 这份指南专门给**没有任何技术背景**的朋友看。每一步都写得像"说明书"一样，
> 你只需要照着做，遇到不懂的名词，文里都有**通俗比喻**。照做就能把服务跑起来。

---

## 一、先认识它：这是个什么工具？

一句话：**它是一个帮你自动在"什么值得买"网站签到、赚金币的小工具。**

打个比方：它就像一个**不知疲倦的小助手**。你平时忙，容易忘记每天去签到领金币；
这个小助手可以替你定时去签到，还能把签到记录、爆料内容都帮你存好。

它有两种"干活模式"：

| 模式 | 名字 | 干什么 | 需要账号吗 |
|---|---|---|---|
| 演示模式 | mock（默认） | 用假数据，让你先看看界面长什么样 | 不需要 |
| 真实模式 | real | 真正去"什么值得买"签到赚金币 | 需要你的登录信息 |

**建议**：第一次先不改任何东西，用演示模式把服务跑起来看看效果，熟悉了再切真实模式。

---

## 二、你需要准备什么？（总清单）

就像做饭前先把食材和锅备好，你需要：

- ✅ 一台能上网的电脑（Windows / Mac / 或一台云服务器都可以）
- ✅ 大约 10～30 分钟时间
- ✅ 一个"什么值得买"账号（**只有**想用真实模式签到时才需要）
- ✅ 能正常安装软件（公司电脑若受限，可能需要找 IT 帮忙）

---

## 三、第一步：安装 Docker（把"厨房"搭好）

### 什么是 Docker？
打个比方：**Docker 就像一个"打包好的独立小厨房"**，里面锅碗瓢盆、调料一应俱全。
你不用在自己电脑上东拼西凑装一堆乱七八糟的东西，直接把这个小厨房搬过来就能"炒菜"。

为什么要它？因为这个工具依赖一些运行环境。Docker 一键把这些环境打包好，
**你完全不用自己装**（也避免装错版本出乱子）。这是最省心、最不容易翻车的方式。

### 🪟 Windows 安装步骤
1. 打开浏览器，访问：`https://www.docker.com/products/docker-desktop/`
2. 点 **Download for Windows**（下载安装包）
3. 双击下载好的安装包，**一路点"下一步"**；如果问到 WSL 相关选项，**全部勾选**
4. 安装完**重启电脑**
5. 桌面会出现一个 **Docker 图标**，双击打开它
6. 看到任务栏出现**鲸鱼图标**，且状态显示 **Running（运行中）**，就成功了

### 🍎 Mac 安装步骤
1. 同样打开上面的网址，点 **Download for Mac**
2. 把 Docker 拖进"应用程序"文件夹
3. 打开它，按屏幕提示点"允许/授权"
4. 顶部菜单栏出现**鲸鱼图标**、显示 Running，就成功了

### 🐧 Linux 安装步骤（简略）
在终端里按官方指引装 `docker` 和 `docker-compose-plugin` 即可，
参考：`https://docs.docker.com/engine/install/`

### 怎么验证装好了？
打开"终端"（Mac）/ "命令提示符"或"Git Bash"（Windows），输入下面这行，回车：

```
docker --version
```

如果看到类似 `Docker version 20.x` 的字样，说明装好了。
再输入 `docker compose version`，也有版本号输出，就万事俱备。

### 常见问题
| 现象 | 原因 | 怎么办 |
|---|---|---|
| 打开 Docker 提示 WSL / 虚拟机错误 | Windows 没开启相关功能 | 搜"Docker WSL 2 安装教程"，按官方步骤开启"虚拟机平台"和"Linux 子系统"后重启 |
| 公司电脑装不上 | 没有管理员权限 | 找 IT 帮忙；或看文末附录"不用 Docker 直接跑" |
| 启动后鲸鱼图标一直橙色/报错 | 没重启或内存不足 | 重启电脑；关闭占用内存大的软件再试 |

---

## 四、第二步：把代码拿到手（获取代码）

代码放在你自己的**私有仓库**里（只有你能看），有两种最简单的拿法。

### 方式一（最推荐，纯图形界面）：用 GitHub Desktop
1. 下载安装 GitHub Desktop：`https://desktop.github.com/`
2. 打开后用**你自己的 GitHub 账号登录**
3. 点 **Clone a repository（克隆仓库）**
4. 在列表里找到你的私有仓库 `zdmclock-clone-`，或把地址 `https://github.com/125540847/zdmclock-clone-.git` 粘贴进去
5. 选一个本地文件夹来存放，点 **Clone**
6. 完成后，在"文件管理器"里打开这个文件夹——这就是你的"工作目录"

### 方式二（最简单，但不能自动更新）：下载压缩包
1. 浏览器登录 GitHub，打开你的仓库页面
2. 点绿色的 **Code** 按钮 → 选 **Download ZIP（下载压缩包）**
3. 把 ZIP 解压到任意文件夹
> 注意：这种方式没有"自动更新"功能，以后想更新只能重新下载。

### 方式三（懂一点命令行的人用）
```
git clone https://github.com/125540847/zdmclock-clone-.git
```

---

## 五、第三步：配置参数（告诉小助手基本设定）

### 概述
代码里有一份"设置模板"文件，名叫 **`.env.example`**。
你**基本不用改它**——启动脚本会自动复制它，生成真正生效的 `.env`。

打个比方：这就像买家具附带的**"安装说明书"**，按出厂默认装好就能用，
大部分开关保持原样即可。

### 一般不用动的设置
| 设置项 | 默认值 | 说明 |
|---|---|---|
| 端口 PORT | 3000 | 小助手的"门牌号"，默认 3000，一般不动 |
| 运行模式 SMZDM_ADAPTER | mock | 默认演示模式，先用假数据看效果 |
| 管理员密码 ADMIN_PASSWORD | admin123 | 默认弱密码，仅本地用；**公网必须改** |

### 只有想真实签到，才需要改这一项
1. 用"记事本"（Windows）或任意文本编辑器，打开文件夹里的 **`.env`** 文件
   （如果还没有 `.env`，先按下一步启动一次，脚本会自动生成它）
2. 找到这一行：`SMZDM_ADAPTER=mock`
3. 把它改成：`SMZDM_ADAPTER=real`
4. **保存**文件

> 真正的账号信息（登录凭证）不是写在这里的，而是在网页里录入，见第八步。

### 安全提醒（重要）
- 如果你只是**自己电脑本地玩**，保持默认即可，不用改密码。
- 如果你要把它放到**公网**让外网能访问，请务必在 `.env` 里：
  - 把 `REQUIRE_AUTH=false` 改成 `REQUIRE_AUTH=true`
  - 把 `ADMIN_PASSWORD=admin123` 改成**一个你自己的强密码**
  - 给 `API_TOKEN=` 填一个强随机值（可搜"随机密码生成器"生成）
- 原因：默认是"方便本地试用"的设定，公网不加固等于大门没锁。

---

## 六、第四步：启动服务（让小助手上岗）

> 好消息：**你不需要手动安装任何依赖**。第一次启动时，Docker 会自动把需要的运行环境装好，
> 这就是用 Docker 的好处——"开箱即用"。

### 🍎 Mac / Linux 用户
1. 打开"终端"，进入你的代码文件夹
   （技巧：在文件夹上右键 → "服务" → "新建位于文件夹位置的终端窗口"）
2. 输入下面这行，回车：
   ```
   ./deploy.sh
   ```
3. 等待几分钟，看到 `部署完成！访问地址 : http://localhost:3000` 就成功了

### 🪟 Windows 用户

**方式 A（推荐，用 Git Bash）：**
1. 先安装 Git for Windows：`https://git-scm.com/`（一路下一步即可）
2. 在代码文件夹的**空白处右键** → 选 **"Git Bash Here"**
3. 在弹出的黑窗口里输入：`./deploy.sh` 回车

**方式 B（如果脚本报错，用 PowerShell）：**
1. 在代码文件夹 **Shift + 右键** → 选 **"在终端中打开"**（打开 PowerShell）
2. 输入下面这行，回车：
   ```
   docker compose up -d --build
   ```
3. 等它跑完，看到 `Container zdmclock Started` 之类提示即可

### 启动后会发生什么？
- **第一次**会下载并"搬进来"运行环境（比喻：第一次搬小厨房，要等几分钟，请耐心）
- 之后启动很快
- 服务会在**后台自动运行**，关掉终端窗口也不影响

### 常见问题
| 现象 | 原因 | 怎么办 |
|---|---|---|
| 提示 permission denied / 拒绝访问 | 脚本没执行权限 | Mac/Linux 先运行 `chmod +x deploy.sh` 再试；Windows 用 Git Bash 一般无此问题 |
| 提示 docker: command not found | Docker 没装好或没重启 | 回到第三步重装并重启 |
| 一直卡在"构建/下载" | 网络慢，首次要下载环境 | 耐心等；确认电脑能正常上网 |
| Windows 用 PowerShell 跑 deploy.sh 报语法错 | `.sh` 文件不能用 PowerShell 直接跑 | 改用 Git Bash（方式 A）或用方式 B 的 `docker compose` 命令 |

---

## 七、第五步：验证结果（确认小助手在上班）

1. 打开浏览器（Chrome / Edge 等都可以）
2. 在地址栏输入：`http://localhost:3000`
   - 如果你是部署在**另一台电脑/服务器**上，把 `localhost` 换成那台机器的 IP，
     例如：`http://192.168.1.100:3000`
3. 看到网页界面（签到日历、爆料列表等）→ 🎉 **成功了！**
4. 想进一步确认后台健康：浏览器打开 `http://localhost:3000/api/health`
   显示 `{"status":"ok"}` 这类字样，说明一切正常

### 常见问题
| 现象 | 原因 | 怎么办 |
|---|---|---|
| 网页打不开 | 服务还在启动 / 地址不对 | 等 10 秒再试；确认地址和端口 3000 正确；运行 `docker compose ps` 看是否在运行 |
| 网页白屏 | 首次构建还没完成 | 等健康检查通过（命令行不再滚动）后再打开 |

---

## 八、进阶：让小助手真正去签到（真实模式 + 录入账号）

> 前提：第六步里已经把 `SMZDM_ADAPTER` 改成了 `real`，并且服务已启动。

1. 打开网页 `http://localhost:3000`
2. 找到网页里的"**设置 / 录入**"相关入口（网页内有文字提示），填入你的"什么值得买"**登录 Cookie**
3. 保存后，小助手就会按设定定时去真实签到、赚金币了

**怎么拿到你的 Cookie（通俗版）：**
1. 用 Chrome 浏览器**登录**"什么值得买"网页版
2. 按键盘 **F12**，打开"开发者工具"
3. 点顶部的 **网络（Network）** 标签
4. 按 **F5** 刷新页面，在左侧点任意一个请求
5. 在右侧找到 **请求头（Headers）**，里面有一行 **Cookie:** ，把后面那一大串**整段复制**
6. 粘回网页里的录入框，保存

> ⚠️ 提醒：Cookie 相当于你的**登录凭证（等于账号密码）**，只存在你本地/容器里，
> **不要发给任何人**，也不要填到不明网站。

---

## 九、日常使用 & 问题速查

| 你想做的事 | 操作命令（在代码文件夹里运行） |
|---|---|
| 停止服务（小助手下班） | `docker compose down` |
| 重新启动 | `docker compose up -d`（或再跑 `./deploy.sh`） |
| 查看运行日志 | `docker compose logs -f` |
| 更新到新版本 | 重新获取最新代码后，再跑 `./deploy.sh`（会自动重建） |

**数据会丢吗？** 不会。你的签到数据存在专门的"储物柜"（技术名：命名卷 `zdmclock-data`）里，
重装、重启、甚至换容器都不丢，也不会和你电脑上的其他文件混在一起。

### 常见问题速查表
| 现象 | 可能原因 | 解决办法 |
|---|---|---|
| 重启电脑后网页打不开 | 服务没自动起来 | 重新运行 `./deploy.sh` 或 `docker compose up -d` |
| 想换端口 | 3000 被别的程序占了 | 在 `.env` 改 `PORT=新数字`，并在 docker-compose.yml 的 ports 同步改，再重建 |
| 真实签到没反应 | Cookie 过期 / 没录入 | 重新按第八步获取并录入最新 Cookie |
| 忘记改了哪些配置 | 不确定 | 删掉 `.env`，重新跑 `./deploy.sh` 会生成一份全新默认配置 |

---

## 十、附录（可选）：不想用 Docker，直接在本机跑

> 只有 Docker 实在装不上时才看这节。这种方式更"原始"，需要你手动装运行环境。

1. 先安装 **Node.js 20 或以上**版本：`https://nodejs.org/`（选 LTS 版，一路下一步）
2. 打开终端，进入代码文件夹，依次运行：
   ```
   cd server
   npm install
   npm run build
   cd ..
   npm install
   npm run build
   ```
3. 配置 `.env`（同第五步）
4. 启动：`npm start`
5. 浏览器打开 `http://localhost:3000`

> 这种方式每次更新都要重复上面的安装步骤，不如 Docker 一键方便，因此**默认推荐用 Docker**。

---

📌 **总结一句话**：装 Docker → 拿代码 → 跑 `./deploy.sh` → 开 `http://localhost:3000`，
零基础也能在十分钟内把小助手请上岗。
